html
    head
        meta(charset="utf-8")
        meta(http-quiv="X-UA-Compatible", content="IE=edge,chrome=1")
        meta(name="view-port", content="width=device-width, initial-scale=1.0, maximum-scale=1.0")
        title!= title
        link(rel="stylesheet", type="text/css", href="#{cdn.host}semantic/dist/semantic.min.css?#{cdn.version}")
        style(type="text/css").
            .cmd {
                -moz-border-radius: 5px;
                -webkit-border-radius: 5px;
                background-color: #202020;
                border: 4px solid silver;
                border-radius: 5px;
                box-shadow: 2px 2px 3px #6e6e6e;
                color: #e2e2e2;
                display: block;
                font: 1em 'andale mono', 'lucida console', monospace;
                line-height: 1em;
                overflow: auto;
                padding: 15px;
            }

            .auto-wrap {
                word-wrap: normal;
                white-space: pre-wrap;
            }

            .hard.auto-wrap {
                word-wrap: break-word;
            }

body
    div(ng-app="cubeModule", ng-controller="cubeCtrl")
        div.ui.two.column.grid
            div.ui.column
                include partial/dash
            div.ui.column
                include partial/cube

    div
        script(src="#{cdn.host}bower_components/jquery/dist/jquery.min.js?#{cdn.version}")
        script(src="#{cdn.host}semantic/dist/semantic.min.js?#{cdn.version}")
        script(src="#{cdn.host}bower_components/angular/angular.min.js?#{cdn.version}")
        script(src="#{cdn.host}virtual-scripts/cube/graph.js?#{cdn.version}")
        script(src="#{cdn.host}virtual-scripts/cube/layout.js?#{cdn.version}")
        script(src="#{cdn.host}virtual-scripts/cube/painter.js?#{cdn.version}")
        script(src="#{cdn.host}virtual-scripts/cube/cube.js?#{cdn.version}")
        script(src="#{cdn.host}virtual-scripts/cube/cube-lite.js?#{cdn.version}")
        script(src="#{cdn.host}virtual-scripts/cube/iterator.js?#{cdn.version}")
        script(src="#{cdn.host}virtual-scripts/cube/solver.js?#{cdn.version}")
        script(src="#{cdn.host}bower_components/cytoscape/dist/cytoscape.min.js?#{cdn.version}")
        script(src="#{cdn.host}scripts/cubeModule.js?#{cdn.version}")
        script(type="text/javascript").